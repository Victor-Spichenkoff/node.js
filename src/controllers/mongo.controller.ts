import { Request, Response } from "express"
import User from "../models/Mongo_User"
 

export const getAllMongo = async () => {
    // const result = await User.find({
    //     idade: {  $gt: 1, $lt: 123 }
    // }).sort({
    //     // campo - ordem
    //     idade: 1//asc
    // })
    // console.log(result)
}